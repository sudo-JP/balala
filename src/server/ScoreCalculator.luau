local ScoreCalculator = {}

function ScoreCalculator.calculateHand(hand, jokers)
    local chip = 0
    local cards = hand.getSelectedHand
    for _, card in ipairs(cards) do
         
    end
end

return ScoreCalculator
